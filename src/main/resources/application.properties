## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url = jdbc:mysql://localhost:3306/royalty-parser-one-to-many?useSSL=false
spring.datasource.username = ianmoreno
spring.datasource.password = 1Bladerz1


## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

## Hibernate ddl auto (create, create-drop, validate, update)
# Allows hibernate to automatically create the database tables from the entity classes on the first run
#spring.jpa.hibernate.ddl-auto = create

# use update after initial db tables are built on first run
spring.jpa.hibernate.ddl-auto = update 
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type=TRACE